---
id: model-genetic-algorithm
title: 遗传算法
english_name: Genetic Algorithm
aliases: [Genetic Algorithm]
tier: high-frequency
category: 智能优化
status: approved
decision: approved_by_user_2026-07-29
code_status: passed_in_build_environment
source_status: foundational_sources_listed
tags: [模型, high-frequency, 智能优化]
use_cases: [非凸、离散或黑箱优化]
code_example: "[[40-Code-Examples/genetic-algorithm/README|Python 示例]]"
last_validated: 2026-07-29
---

# 遗传算法

## 一句话定位

通过选择、交叉和变异进行群体搜索。

## 适用问题

非凸、离散或黑箱优化。系统只能把本方法列为候选，不能仅凭关键词直接确定为主模型。

## 前置条件与数据要求

结果随机且参数较多，不保证全局最优。使用前应检查字段含义、样本量、量纲、缺失值、异常值和数据泄漏风险。

## 数学原理

编码种群并迭代进化。正式论文需要结合当前题目的变量、约束与假设重新推导，不能只粘贴通用公式。

## 标准流程

先建立数据与目标定义，再运行简单基线；随后拟合或求解本模型，使用同一数据、同一划分和同一指标与候选模型比较；最后进行稳健性、灵敏度或误差分析，并记录配置、数据哈希、随机种子和运行环境。

## 评价与检验

重点检查：多次运行均值方差、收敛曲线。复杂或随机模型必须报告多次运行或交叉验证的平均结果和波动。

## 优点

该方法具有明确的经典定义，并拥有成熟的 Python 实现路径。其优势只有在当前题目的数据条件和目标下得到验证后，才能写入论文结论。

## 局限与不适用场景

方法假设不满足、样本规模不足、变量定义不清或缺乏有效验证时，不应强行套用。不能以“模型更高级”代替必要性论证。

## 相近模型

通过同类别 MOC、题型页和双向链接比较相近方法。主模型选择需要同时保留被淘汰模型及淘汰理由。

## Python 示例

见 [[40-Code-Examples/genetic-algorithm/README|遗传算法可运行示例]]。示例用于验证基本机制，不代表已经复现任何具体论文或国赛题。

## 论文写法

论文中应依次说明选择理由、变量与假设、模型表达、求解方法、参数来源、结果与评价、稳健性和局限。所有结果必须引用冻结文件，不能手工抄写未追溯数字。

## 来源与证据

- Holland, *Adaptation in Natural and Artificial Systems*；MathWorks GA 官方文档。
- [[2024B-P02-蚁群遗传蒙特卡洛生产决策-审计笔记]]。

## 随机适应度审核增量

当适应度由 Monte Carlo 给出时，单次模拟会把噪声当成遗传优势。应使用固定评价样本或共同随机数、每个个体多次重复、置信区间或序贯加样；精英也不能每代被不受控的新噪声任意重排。至少报告 30 个随机种子的最优值、均值、标准差、可行率和耗时，并与枚举/MILP/DP 基线比较 gap。16 位二元空间只有 65536 个策略时，必须解释使用 GA 而非枚举的必要性。

本条已由用户于 2026-07-29 批准，并合并 2024 B 题审计增量。
