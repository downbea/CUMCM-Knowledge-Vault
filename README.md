# 国赛数学建模知识库

本 Vault 是正式长期知识源。论文原件、模型卡、代码示例、题型索引、写作规律和比赛复盘通过双向链接、标签、YAML 元数据和本地混合检索关联。

知识状态：

- `pending`：解析中或待修复，不参与正式检索。
- `seeded`：系统预置并完成代码验证，但仍等待首次人工集中审核。
- `approved`：人工确认通过，进入正式检索。
- `deprecated`：保留历史，但默认不推荐。
